T-031 — Locations Module implementation is complete. Here's what was built:

    Summary of Implementation

    ✅ Acceptance Criteria Met

     1. Location hierarchy renders correctly (Region → Country → City → Office)
        - Table view with type badges and parent relationships
        - Tree/Hierarchy view with collapsible nodes showing full hierarchy
        - Both views accessible via tabs on the Locations page

     2. IANA timezone saved and displayed with correct local time preview
        - Timezone field in Location model with IANA format validation
        - Dropdown with common IANA timezones in the form
        - Real-time local time preview shown while editing (updates as timezone changes)
        - Local time displayed in both table and tree views

     3. Headquarters flag: only one location per company can be HQ (validated at API)
        - is_headquarters boolean field in Location model
        - Mongoose pre-save hook validates only one HQ per company (throws error if duplicate)
        - UI warning shown when HQ checkbox is selected
        - HQ badge displayed in table and tree views

     4. Deleting a location blocked if users are assigned (409 with count)
        - Backend checks User.countDocuments() before deletion
        - Returns 409 with LOCATION_HAS_USERS error code and user count
        - Delete button disabled when users are assigned (tooltip shows count)
        - Error toast displays the specific message with user count

     5. `location_id` added to User model — shown in People page user detail
        - Already existed in User model and schema
        - Already populated in people controller (populate('location_id', 'name'))
        - Already displayed in UserDetailPage (user.location?.name)

     6. All mutations produce audit events
        - location.created - logged on creation
        - location.updated - logged on update
        - location.deleted - logged on deletion
        - All include before/after state, actor info, IP, user agent

    Files Created/Modified

    Backend:
     - ✅ Updated server/src/models/Location.model.ts - Added HQ validation hook
     - ✅ Created server/src/controllers/locations.controller.ts - Full CRUD with audit logging
     - ✅ Created server/src/routes/locations.routes.ts - Express routes with auth/role guards
     - ✅ Updated server/src/routes/index.ts - Registered /locations routes

    Frontend:
     - ✅ Updated client/src/types/index.ts - Added Location, LocationTreeNode, CreateLocationInput, UpdateLocationInput,   
       LocationFilters
     - ✅ Updated client/src/constants/queryKeys.ts - Added LOCATIONS, LOCATION_TREE, LOCATION_DETAIL
     - ✅ Created client/src/features/locations/hooks/ - 5 hooks (useLocations, useLocationTree, useLocationDetail,
       useCreateLocation, useUpdateLocation, useDeleteLocation)
     - ✅ Created client/src/features/locations/components/ - 3 components (LocationForm, LocationTable, LocationHierarchy) 
     - ✅ Created client/src/pages/locations/LocationsPage.tsx - Full page with all 4 states
     - ✅ Updated client/src/App.tsx - Added /locations route
