Perfect! T-025 — Org Structural Health UI + Org Change History has been successfully implemented! Here's the
    comprehensive verification:

    ✅ Acceptance Criteria - All Met:

    1. ✅ /organization/health returns correctly grouped insights
     - Backend endpoint: GET /organization/health in organization.controller.ts
     - Response shape:

      1 {
      2   insights: {
      3     critical: Insight[],
      4     warning: Insight[],
      5     info: Insight[]
      6   },
      7   counts: {
      8     critical: number,
      9     warning: number,
     10     info: number,
     11     total: number
     12   }
     13 }
     - Query: Fetches unresolved insights scoped to company_id, grouped by severity

    2. ✅ OrgHealthPage shows all 3 severity groups with correct counts
     - Component: OrgHealthTab.tsx
     - UI: Three summary cards at top showing Critical, Warning, Info counts with color-coded badges
     - Grouped rendering: Insights rendered under each severity header with count: "Critical (3)", "Warning (5)", "Info (2)"
     - Visual design: Severity-specific background colors, borders, and icons following design tokens

    3. ✅ "Fix Now →" deep link navigates to the correct department or team record
     - Handler: handleNavigateToHealthRecord(insight) in OrganizationPage
     - Implementation: Uses navigate(insight.remediation_url) from react-router-dom
     - Deep links: /organization/:id for departments, /teams/:id for teams, /people/:id for users
     - UX: Clicking "Fix Now →" takes user directly to the affected record for remediation

    4. ✅ Dismiss marks insight is_resolved: true and removes it from view
     - Hook: useDismissInsight() calls PUT /intelligence/:id/resolve
     - Backend: Existing resolveInsight() endpoint in intelligence.controller.ts sets is_resolved: true and resolved_at: new
        Date()
     - Frontend: On success, invalidates ORG_HEALTH and INSIGHTS queries, toast shows "Insight dismissed"
     - UI Update: Dismissed insight immediately removed from view via query refetch

    5. ✅ Empty state shows when no issues exist
     - Component check: if (!data || data.counts.total === 0) renders EmptyState
     - Message: "All clear! No organizational health issues detected. Great job!"
     - Icon: Info icon with primary color accent

    6. ✅ History tab shows all department and team mutations
     - Backend endpoint: GET /organization/history in organization.controller.ts
     - Query: Filters AuditEvent by company_id and module: 'organization'
     - Events captured: department.created, department.updated, department.moved, department.archived, team.created,        
       team.updated, team.deleted, team.member_added, team.member_removed, etc.
     - Sorted: Most recent first (created_at: -1), limited to 100 events

    7. ✅ Filters (object type, date range) work and combine
     - Component: OrgHistoryTab.tsx
     - Filters:
       - Object type dropdown: All Types, Departments, Teams, Team Members
       - Date range: date_from and date_to date pickers
       - Clear filters button when filters active
     - Backend: Query params passed to API, server-side filtering combines all filters:

     1 if (object_type) query.object_type = object_type;
     2 if (date_from || date_to) query.created_at = { $gte, $lte };

    8. ✅ Row expand shows before/after diff (reuse AuditLog diff component)
     - Expand pattern: Click row to expand, shows before/after JSON diff
     - UI: Two-column grid with "Before" and "After" headers, <pre> blocks showing JSON.stringify(state, null, 2)
     - Pattern reuse: Follows exact same pattern as AuditLogTable.tsx expandable rows
     - Visual: Expanded row highlighted with bg-primaryLight, before/after in bordered code blocks

    9. ✅ RULE-08, RULE-09 also wired to fire after org mutations (add to intelligence runner in T-005)
     - RULE-08: Team with no team lead assigned
       - Detection: Finds all active teams where team_lead_id is null/missing
       - Severity: Warning
       - Auto-resolve: When team gets a lead assigned
     - RULE-09: Business Unit with no child departments
       - Detection: Finds all BUs that have no departments pointing to them as parent_id
       - Severity: Info
       - Auto-resolve: When a department is added under the BU
     - Both rules: Added to runIntelligenceRules() in server/src/lib/intelligence.ts
     - Wired to fire: All org mutations (create/update/delete department/team/BU) call
       runIntelligenceRules(req.user.company_id) after completing

    Files Created/Modified:

    Backend (Server):
     - Modified server/src/controllers/organization.controller.ts:
       - Added getOrgHealth() - returns grouped insights
       - Added getOrgHistory() - returns audit events with filters
       - Imported AuditEvent and Insight models
     - Modified server/src/lib/intelligence.ts:
       - Added RULE-08: Team without team lead
       - Added RULE-09: BU without child departments
       - Added auto-resolve logic for both rules
     - Modified server/src/routes/organization.routes.ts:
       - Added GET /health and GET /history routes

    Frontend (Client):
     - Created client/src/features/organization/hooks/useOrgHealth.ts - fetches health data
     - Created client/src/features/organization/hooks/useOrgHistory.ts - fetches history with filters
     - Created client/src/features/organization/components/OrgHealthTab.tsx - health insights UI
     - Created client/src/features/organization/components/OrgHistoryTab.tsx - history with expandable rows
     - Modified client/src/constants/queryKeys.ts - Added ORG_HEALTH, ORG_HISTORY
     - Modified client/src/pages/organization/OrganizationPage.tsx - Added Health and History tabs

    Code Quality:
     - ✅ No any types in frontend code
     - ✅ All hooks typed correctly with explicit generics
     - ✅ All API responses explicitly typed
     - ✅ Zod validation on all endpoints (where applicable)
     - ✅ JSDoc comments on all exported functions
     - ✅ Follows all frontend guidelines and patterns
     - ✅ TypeScript compiles without errors
     - ✅ All queries scoped to req.user.company_id
     - ✅ Audit logging on all mutations (pre-existing)
     - ✅ Intelligence rules auto-resolve when conditions fixed
