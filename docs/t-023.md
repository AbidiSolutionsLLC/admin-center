Perfect! T-023 — Business Unit Management + Org Hierarchy Enhancement has been successfully implemented! Here's the     
    comprehensive verification:

    ✅ Acceptance Criteria - All Met:

    1. ✅ Business Units tab shows all BUs with dept + team counts
     - Endpoint: GET /organization/business-units returns BUs with dept_count and team_count
     - UI: BusinessUnitsTab component displays table with columns: Business Unit, Manager, Departments (count badge), Teams 
       (count badge), Actions
     - Hook: useBusinessUnits() fetches data with proper caching

    2. ✅ BU tree expands to show full hierarchy: BU → Departments → Teams
     - Endpoint: GET /organization/bu-tree returns full nested hierarchy
     - UI: BUTreeView component with expandable/collapsible nodes
     - Visual structure:
       - Level 0: Business Units (amber highlight)
       - Level 1: Departments (gray background)
       - Level 2: Teams (with team lead info)
     - Hook: useBUTree() fetches hierarchy data

    3. ✅ Create BU form defaults dept_type to 'business_unit' and hides type selector
     - BUForm component: No type selector - type is locked to 'business_unit'
     - useCreateBU hook: Automatically adds type: 'business_unit' to the payload
     - Form fields: Only shows Name, Primary Manager ID, Secondary Manager ID
     - Validation: Zod schema enforces required name, optional valid MongoDB ObjectIds for managers

    4. ✅ BU deletion blocked if it has child departments (409 error + user-friendly message)
     - Backend: DELETE /organization/business-units/:id checks for child departments
     - Error handling: Returns 409 with message: "Cannot delete Business Unit "X" — it has Y child department(s). Remove or 
       reassign all child departments first."
     - Frontend: useDeleteBU hook catches BU_HAS_CHILD_DEPARTMENTS error code and shows user-friendly toast
     - ConfirmDialog: Shows before deletion attempt

    5. ✅ UserOrgAssignmentModal saves dept + teams in one request
     - Endpoint: POST /people/:id/assign-org accepts department_id and team_ids in one request
     - UI: Modal with department dropdown and team checkboxes (filtered by selected department)
     - Component: UserOrgAssignmentModal in client/src/features/people/components/
     - Hook: useAssignUserOrg mutation sends both assignments together

    6. ✅ After assignment, user's department and team memberships update immediately (React Query invalidation)
     - React Query invalidation in useAssignUserOrg:

     1   queryClient.invalidateQueries({ queryKey: QUERY_KEYS.USERS });
     2   queryClient.invalidateQueries({ queryKey: QUERY_KEYS.USER_DETAIL(variables.userId) });
     - Toast feedback: "User organization assignment updated successfully"
     - Modal closes on success, triggering UI refresh

    Files Created/Modified:

    Backend (Server):
     - Modified server/src/controllers/organization.controller.ts:
       - Added getBUTree() - full hierarchy endpoint
       - Added getBusinessUnits() - BU list with counts
       - Added deleteBusinessUnit() - with child department check (409)
       - Added assignUserOrg() - user org assignment endpoint
     - Modified server/src/routes/organization.routes.ts - added BU routes
     - Modified server/src/routes/people.routes.ts - added assign-org route

    Frontend (Client):
     - Created client/src/features/organization/hooks/useBusinessUnits.ts
     - Created client/src/features/organization/hooks/useBUTree.ts
     - Created client/src/features/organization/hooks/useCreateBU.ts
     - Created client/src/features/organization/hooks/useDeleteBU.ts
     - Created client/src/features/people/hooks/useAssignUserOrg.ts
     - Created client/src/features/organization/components/BusinessUnitsTab.tsx
     - Created client/src/features/organization/components/BUForm.tsx
     - Created client/src/features/organization/components/BUTreeView.tsx
     - Created client/src/features/people/components/UserOrgAssignmentModal.tsx
     - Modified client/src/constants/queryKeys.ts - Added BUSINESS_UNITS, BU_TREE
     - Modified client/src/pages/organization/OrganizationPage.tsx - Added Business Units tab

    Code Quality:
     - ✅ No any types in frontend code
     - ✅ All hooks typed correctly with explicit generics
     - ✅ All API responses explicitly typed
     - ✅ Zod validation on all forms and endpoints
     - ✅ JSDoc comments on all exported functions
     - ✅ Follows all frontend guidelines and patterns
     - ✅ TypeScript compiles without errors
     - ✅ All mutations produce audit log entries
     - ✅ All queries scoped to req.user.company_id