Perfect! Implementation complete. Here's a summary:

    ✅ H-011: Security MongoDB Schema - IMPLEMENTED

    What was built:

    1. SecurityEvent Model (server/src/models/SecurityEvent.model.ts)
     - Comprehensive schema for logging all security-related events
     - Tracks: login_attempt, login_success, login_failure, logout, password_reset_*, mfa_*, session_expired, token_*,      
       suspicious_activity_detected, account_locked/unlocked
     - Fields: company_id, user_id, email, event_type, ip_address, user_agent, is_suspicious, metadata
     - Indexed for efficient querying by company, event type, email, and suspicious flag

    2. SecurityPolicy Model (server/src/models/SecurityPolicy.model.ts)
     - One policy per company (unique constraint)
     - Default settings include:
       - max_failed_login_attempts: 5
       - lockout_duration_minutes: 30
       - session_timeout_minutes: 480 (8 hours)
       - Password complexity requirements
       - MFA toggle
       - IP whitelist support

    3. Auth Controller Updates (server/src/controllers/auth.controller.ts)
     - ✅ Logs `SecurityEvent` on EVERY login attempt (attempt, success, failure)
     - ✅ `is_suspicious` flag logic: Automatically set to true on the 5th consecutive failure within a 30-minute window    
     - Checks SecurityPolicy for the company's max_failed_login_attempts threshold
     - Failures for both existing and non-existing users are tracked

    4. SecurityPolicy Seeder (server/src/lib/seed.ts)
     - seedSecurityPolicy(companyId) function creates default policy for each company
     - Integrated into seedDatabase() - runs automatically during seeding
     - Idempotent: won't create duplicates if policy already exists

    5. Test Script (server/src/scripts/test-security-schema.ts)
     - Verifies all acceptance criteria pass:
       - ✅ SecurityEvent logged on login attempts
       - ✅ is_suspicious flag correctly set after 5 failures (4 non-suspicious, 1 suspicious)
       - ✅ SecurityPolicy seeded with correct defaults

    Acceptance Criteria - ALL MET:
     - ✅ SecurityEvent logged on every login attempt
     - ✅ is_suspicious flag set correctly (tested with 5 manual failures)
     - ✅ SecurityPolicy model seeded with defaults for each company on creation

    All TypeScript compilation passes. Ready for review! 🎉